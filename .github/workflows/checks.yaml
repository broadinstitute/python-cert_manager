---
name: 'checks'

'on':
  pull_request:
    branches:
      - 'main'
  push:
    branches:
      - 'main'

jobs:
  linting:
    uses: broadinstitute/shared-workflows/.github/workflows/python-linting.yml@v1.2.1
  unit-tests:
    uses: broadinstitute/shared-workflows/.github/workflows/python-unit-tests.yml@v1.2.1
    with:
      python_package_name: cert_manager
